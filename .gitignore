# Secrets — never commit
.env

# Virtual envs / build
.venv/
venv/
build/
dist/
*.egg-info/
src/*.egg-info/

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Downloaded corpora (fetched on demand; not redistributed)
data/corpora/

# Precomputed embedding index — ~150 MB build artifact, over GitHub's 100 MB
# limit. Fetched on first run via `pleio-hpo download`, not committed.
src/pleio_hpo/data/hpo/index*.npz

# Trained validator checkpoint — PubMedBERT (~440 MB), over GitHub's 100 MB limit.
# Fetched on first run via `pleio-hpo download`, not committed.
src/pleio_hpo/data/validator/

# Local model cache
.pleio-hpo/
.DS_Store
